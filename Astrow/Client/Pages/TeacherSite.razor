@page "/teachersite"
@using Astrow.Shared.DTO
@using Astrow_Services.Services;

<h3>Teacher Site</h3>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in students)
        {
            <tr>
                <td>@student.StudentId</td>
                <td>@student.Unilogin</td>
                <td>@student.Name</td>
                <td>
                    <button class="btn btn-danger" @onclick="() => DeleteStudent(student.StudentId)">Delete</button>
                    <button class="btn btn-primary" @onclick="() => UpdateStudent(student.StudentId)">Update</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    private List<StudentDTO> students = new List<StudentDTO>();

    public void DeleteStudent(Guid studentid)
    {

    }
    public void UpdateStudent(Guid studentid)
    {

    }
}
